# requirements-a14b.txt — ambiente per Wan2.2-T2V-A14B
#
# Vive da solo perche' la model card di Wan-AI/Wan2.2-T2V-A14B-Diffusers
# (verificata il 15/09/2026) dichiara: "requires features that are currently
# available only in the main branch of diffusers". La 0.40.0 di
# requirements-gen.txt non basta, e installare diffusers da main nell'ambiente
# dei generatori metterebbe a rischio la riproducibilita' degli altri adapter.
#
#   pip install -r requirements-a14b.txt --extra-index-url https://download.pytorch.org/whl/cu128
#
# PIN FATTO il 17/09/2026 sul pod A100: "main" risolveva a
# 37ce5add81f5b0838b744f03dcc2238ffc740cbc, cioe' diffusers 0.41.0.dev0, e da
# allora e' quello lo SHA scritto qui sotto. Con "@main" due sessioni diverse
# avrebbero girato su due diffusers diversi e il confronto del bake-off avrebbe
# perso una variabile controllata.
#
# huggingface_hub NON e' 1.28.0 come negli altri due ambienti: diffusers
# 0.41.0.dev0 dichiara "huggingface-hub<2.0 and >=1.31.0", e con 1.28.0 pip si
# ferma con ResolutionImpossible. Gli ambienti sono separati, quindi la
# differenza non tocca gli altri adapter.

torch==2.11.0+cu128
torchvision==0.26.0+cu128
numpy==2.4.6

git+https://github.com/huggingface/diffusers@37ce5add81f5b0838b744f03dcc2238ffc740cbc
transformers==5.15.1
accelerate==1.14.0
tokenizers==0.22.2
safetensors==0.8.0
huggingface_hub==1.31.0
hf-xet==1.6.0

ImageIO==2.37.4
imageio-ffmpeg==0.6.0
pillow==12.3.0
psutil==7.2.2
ftfy==6.3.1
PyYAML==6.0.3

# Per il gate in linea di runner.py, che chiama quality_gate.evaluate_clip() su
# CPU mentre la GPU fa la clip successiva. Variante headless: sul pod non c'e'
# un server grafico, e quella completa trascina libGL. Senza cv2 il runner non
# si ferma, ma registra gate_sharpness e gate_ssim vuoti.
opencv-python-headless==4.11.0.86
