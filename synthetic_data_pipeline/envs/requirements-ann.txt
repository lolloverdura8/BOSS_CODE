# requirements-ann.txt — ambiente degli annotatori (annotate_sam.py, annotate_da3.py)
#
# In locale SAM 3 e DA3 stanno in due venv separati, sam3_1/ (Python 3.12.10) e
# depth_anything3/ (Python 3.11.9). Sul pod possono stare insieme, e non e' una
# scommessa: verificato il 15/09/2026 confrontando i due pip freeze, i 70
# pacchetti in comune hanno versioni IDENTICHE, zero conflitti. La separazione
# locale era casuale, non un vincolo. I due pacchetti upstream lo confermano:
# sam3 chiede python>=3.8, depth_anything_3 chiede >=3.9,<=3.13.
#
# Il vincolo vero e' un altro: qui numpy deve stare SOTTO la 2, mentre i
# generatori vogliono la 2.4.6. E' per questo che gen e ann restano due venv.
#
#   pip install -r requirements-ann.txt --extra-index-url https://download.pytorch.org/whl/cu128
#
# Poi i due pacchetti upstream, che sono install editabili da git e vanno clonati
# ai commit misurati (li installa setup_pod.sh):
#   sam3             8f0b7f4d4e7eda2ed606ebde6702c93359ad01da
#   depth_anything_3 3d835ec1a5802d64a8b8b15f817a1ab54809bfe4
# Ognuno tira le proprie dipendenze transitive: qui stanno solo i pin che contano.
#
# Escluso dal freeze: colorama, pywinpty, triton-windows (solo Windows).

torch==2.11.0+cu128
torchvision==0.26.0+cu128

# Il pin che divide questo ambiente da quello dei generatori.
# sam3/pyproject.toml: "numpy>=1.26,<2". Depth-Anything-3/pyproject.toml: "numpy<2".
numpy==1.26.4

opencv-python==4.11.0.86
pillow==12.3.0
ImageIO==2.37.4
scipy==1.17.1
einops==0.8.2
safetensors==0.8.0
huggingface_hub==1.28.0

# SAM 3
timm==1.0.28

# Depth Anything 3. pycolmap e' nella catena di import di depth_anything_3.api
# (api -> utils.export -> colmap -> pycolmap): non e' opzionale, senza non si
# importa il modello. Su questa workstation Smart App Control ne blocca la DLL
# _core, che e' il motivo per cui annotate_da3.py non parte in locale.
pycolmap==4.1.1
pillow_heif==1.5.0

psutil==7.2.2
tqdm==4.70.0
